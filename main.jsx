import React from 'react';
import ReactDOM from 'react-dom/client';
import AuditEngine from './src/AuditEngine.jsx';

ReactDOM.createRoot(document.getElementById('root')).render(
  <React.StrictMode>
    <AuditEngine />
  </React.StrictMode>,
);
